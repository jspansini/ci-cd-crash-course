name: First Workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: ubunto-latest
    steps:
      - name: Print greeting
        run: echo "HELLO MRS. BYTE"
      - name: Print bye
        run: echo "by bye"
